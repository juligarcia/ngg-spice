---
instance_name: C
symbol: capacitor.svg

dimensions:
  width: 100
  height: 100

top_ports: []

right_ports:
  - label: "right"

bottom_ports: []

left_ports:
  - label: "left"

fields:
  - type: string
    name: value
    required: true
